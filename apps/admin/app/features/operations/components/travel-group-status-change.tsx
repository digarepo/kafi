import type { TravelGroup } from '../../../lib/api.js';

type TravelGroupStatusChangeProps = {
  group: TravelGroup;
  onChanged: () => void;
};

export function TravelGroupStatusChange(_props: TravelGroupStatusChangeProps) {
  return null;
}
